name: klondike_table
description: Phone prototype matching the v1 spec (felt-banner chrome + table).
publish_to: none
version: 0.0.0

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  lints: ^6.1.0

flutter:
  uses-material-design: false
