# Write the v1 spec from the closed decisions?

Type: task
Status: resolved
Blocked by: 15

## Question

The look, rules, chrome, and store-adjacent contents for v1 are decided. Assemble `.scratch/klondike-solitaire-spec/spec.md` from the closed tickets on this map so a later session can implement without reconstructing the spec from each answer.

Do not reopen those decisions. Do not implement the app. Ticket `07` (iOS local prototype) is unrelated and can stay waiting on a MacBook.

## Answer

Assembled [spec.md](../spec.md) from the closed tickets. It is the implementable v1 spec: product, rules, interaction, screens, look (felt banner), and engineering constraints. Decisions still live on their tickets; the spec compiles them and points back.

Did not reopen those answers. Did not implement the app. [Can we build and install the Klondike table prototype on iOS locally?](issues/07-ios-local-prototype-build.md) stays open, waiting on a MacBook.
